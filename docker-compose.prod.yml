services:
  backend:
    depends_on:
      redis:
        condition: service_healthy
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgres://propremiumcare:Mvsc2026%23%2356@database-1.c5wkcis2qg1p.ap-south-1.rds.amazonaws.com:5432/prepreimiumcare_ecommerce?ssl=true&sslmode=no-verify
      - REDIS_URL=redis://redis:6379

  storefront:
    depends_on:
      backend:
        condition: service_healthy
    environment:
      - NODE_ENV=production
      - MEDUSA_BACKEND_URL=http://procare_backend:9000
      - NEXT_PUBLIC_MEDUSA_BACKEND_URL=https://propremiumcare.com
      - NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY=pk_b0890f83a696f8c2238795063abafa10b07f4e5622a64c10c63c9fcbf3412613

  nginx:
    build:
      context: .
      dockerfile_inline: |
        FROM nginx:alpine
        COPY nginx.conf /etc/nginx/conf.d/default.conf
    volumes: []
    ports:
      - "9000:80"
